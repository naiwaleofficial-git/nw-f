# Local dev can be left blank; the app defaults to http://localhost:5000/api.
# In production, set this to your deployed backend URL, for example:
# VITE_API_URL=https://your-backend.example.com/api
VITE_API_URL=
